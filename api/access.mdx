---
title: "Solicitar acesso à API"
description: "Como pedir acesso ao beta fechado da API dos Motores de Análise"
icon: "unlock"
---

A API `/v1` dos [Motores de Análise](/motores/introducao) está em **beta fechado**. O acesso é liberado por workspace, mediante solicitação ao time Sherlocker, e cobre os dois motores: o de **CPF/CNPJ** (`/v1/analyses`) e o de **borderô** (`/v1/operations`).

## Features de acesso

O acesso é controlado por duas features, habilitadas por workspace:

| Feature | O que libera |
|---------|--------------|
| `analysis_engine` | Qualquer chamada à API `/v1` — análises de CNPJ e operações de borderô |
| `analysis_engine_pf` | Análises de CPF (`subject_type: "pf"`) — exigida **além** de `analysis_engine` |

Sem a feature necessária, a API responde `403` com o código `feature_not_enabled`:

```json
{
  "type": "https://docs.sherlocker.com.br/problems/feature_not_enabled",
  "title": "Feature Not Enabled",
  "status": 403,
  "code": "feature_not_enabled",
  "detail": "The analysis_engine feature is not enabled for this workspace. To request access visit https://docs.sherlocker.com.br/api/access or contact support at https://docs.sherlocker.com.br/support.",
  "request_access_url": "https://docs.sherlocker.com.br/api/access",
  "instance": "/v1?request_id=req-uuid-here"
}
```

## Como solicitar

<Steps>
  <Step title="Entre em contato">
    Fale com o time Sherlocker. Veja como acionar o time em [Suporte](/support).
  </Step>
  <Step title="Informe os dados do pedido">
    Inclua na mensagem:

    - **Nome do workspace** que deve receber o acesso
    - **Caso de uso** (o que você pretende analisar e em que fluxo — ex.: onboarding/KYC com CNPJ/CPF, esteira de FIDC com borderôs CNAB 400)
    - Se precisa de análises de **PJ (CNPJ)**, **PF (CPF)** ou ambas — análises de CPF exigem a feature adicional `analysis_engine_pf`
  </Step>
  <Step title="Aguarde a liberação">
    O time habilita as features no seu workspace e confirma pelo mesmo canal.
  </Step>
</Steps>

## Depois do acesso

Com as features habilitadas, crie uma chave de API no app Sherlocker (Settings → API Keys) — a mesma chave serve os dois motores — e siga o quickstart do motor que você vai usar.

## Próximos passos

<CardGroup cols={2}>
  <Card title="Quickstart — CPF/CNPJ" icon="rocket" href="/motor-analise/quickstart">
    Rode sua primeira análise em minutos
  </Card>
  <Card title="Quickstart — Borderô" icon="file-invoice-dollar" href="/credito/quickstart">
    Analise seu primeiro borderô CNAB 400
  </Card>
  <Card title="Autenticação" icon="key" href="/motor-analise/autenticacao">
    Como usar sua chave `slhk_` no header Authorization
  </Card>
  <Card title="Suporte" icon="headset" href="/support">
    Como falar com o time Sherlocker
  </Card>
</CardGroup>
