---
title: "Títulos e NFe"
description: "Referência do shape de título no resultado, os estados de nfe_status e a validação cruzada com o ZIP de NFe"
icon: "file-invoice"
---

Quando a operação chega a `completed`, o campo `titulos[]` do `GET /v1/operations/{id}` traz um item por título do borderô. Esta página é a referência desse shape, dos estados de `nfe_status` e da validação cruzada com os XMLs de NFe.

## Shape de um título

```json
{
  "seu_numero": "DUP-0001",
  "sacado_document": "12345678000190",
  "valor": 1234.56,
  "vencimento": "2026-08-01",
  "verdict": "aprovado",
  "nfe_status": "validada",
  "cedente_analysis_id": "a1b2c3d4-0000-0000-0000-000000000010",
  "sacado_analysis_id": "a1b2c3d4-0000-0000-0000-000000000011"
}
```

| Campo | Tipo | Descrição |
|-------|------|-----------|
| `seu_numero` | string | Identificador do título no arquivo remessa (campo "seu número" do CNAB 400) |
| `sacado_document` | string | Documento do sacado (apenas dígitos), extraído do CNAB |
| `valor` | number | Valor do título |
| `vencimento` | string | Data de vencimento (`YYYY-MM-DD`) |
| `verdict` | enum | `aprovado` \| `alerta` \| `reprovado` — veredito consolidado do título (veja [Ciclo de vida](/credito/ciclo-de-vida)) |
| `nfe_status` | enum | Resultado do cruzamento com a NFe — tabela abaixo |
| `cedente_analysis_id` | UUID | Análise do cedente no Motor de Análise (a mesma para todos os títulos da operação) |
| `sacado_analysis_id` | UUID | Análise do sacado no Motor de Análise (compartilhada entre títulos do mesmo sacado) |

`cedente_analysis_id` e `sacado_analysis_id` são análises **normais**: consulte `GET /v1/analyses/{id}` para o detalhamento completo (`verdict`, `blocks`, `coverage`) de cada entidade, no formato do [Motor de Análise](/motor-analise/ciclo-de-vida).

## Validação cruzada com NFe

O campo opcional `nfe_zip_base64` do `POST /v1/operations` recebe um **ZIP com os XMLs de NFe** dos títulos, em base64 (máximo de 32 MB decodificado). Quando enviado, o motor procura o XML correspondente a cada título e cruza os dados do CNAB com os da nota — a duplicata bate com uma NFe real?

<CodeGroup>
```bash cURL
CNAB_B64=$(base64 -i remessa-0605.rem | tr -d '\n')
NFE_B64=$(base64 -i nfes-0605.zip | tr -d '\n')

curl -X POST "https://<API_HOST>/v1/operations" \
  -H "Authorization: Bearer slhk_sua_chave_aqui" \
  -H "Content-Type: application/json" \
  -H "Idempotency-Key: $(uuidgen)" \
  -d "{
    \"cnab_base64\": \"$CNAB_B64\",
    \"nfe_zip_base64\": \"$NFE_B64\",
    \"file_name\": \"remessa-0605.rem\"
  }"
```

```python Python
import base64
import uuid
import requests

def b64(path: str) -> str:
    with open(path, "rb") as f:
        return base64.b64encode(f.read()).decode()

resp = requests.post(
    "https://<API_HOST>/v1/operations",
    headers={
        "Authorization": "Bearer slhk_sua_chave_aqui",
        "Idempotency-Key": str(uuid.uuid4()),
    },
    json={
        "cnab_base64": b64("remessa-0605.rem"),
        "nfe_zip_base64": b64("nfes-0605.zip"),
        "file_name": "remessa-0605.rem",
    },
)
print(resp.status_code, resp.json())
```

```javascript Node.js
import { readFileSync } from "node:fs";

const b64 = (path) => readFileSync(path).toString("base64");

const resp = await fetch("https://<API_HOST>/v1/operations", {
  method: "POST",
  headers: {
    Authorization: "Bearer slhk_sua_chave_aqui",
    "Content-Type": "application/json",
    "Idempotency-Key": crypto.randomUUID(),
  },
  body: JSON.stringify({
    cnab_base64: b64("remessa-0605.rem"),
    nfe_zip_base64: b64("nfes-0605.zip"),
    file_name: "remessa-0605.rem",
  }),
});
console.log(resp.status, await resp.json());
```
</CodeGroup>

<Note>
  Durante o beta, substitua `<API_HOST>` pelo host fornecido pelo time Sherlocker. O host definitivo será publicado na promoção a produção.
</Note>

<Tip>
  O ZIP de NFe entra no hash canônico da idempotência (SHA-256 do conteúdo): reenviar o mesmo CNAB com um ZIP diferente e a mesma `Idempotency-Key` resulta em `409 idempotency_conflict`. Veja [Idempotência](/credito/idempotencia).
</Tip>

## Estados de `nfe_status`

| `nfe_status` | Significado |
|--------------|-------------|
| `validada` | XML correspondente encontrado no ZIP e os dados conferem com o título do CNAB |
| `divergente` | XML correspondente encontrado, mas os dados divergem dos do CNAB — confira o título antes de aprovar |
| `nao_encontrada` | ZIP enviado, mas nenhum XML do pacote corresponde a este título |
| `nao_aplicavel` | A validação de NFe não se aplica a este título — por exemplo, a operação foi criada sem `nfe_zip_base64` |

Como esses estados afetam o `verdict` do título depende da configuração da [engine de borderô](/credito/engines): a engine define se uma divergência ou ausência de NFe gera alerta ou bloqueio.

<Warning>
  `nao_aplicavel` não é um problema — é a ausência de validação. Se a sua esteira exige lastro de NFe para todos os títulos, envie sempre o `nfe_zip_base64` e trate `nao_encontrada` e `divergente` na sua regra de aprovação.
</Warning>

## Limites dos arquivos

| Campo | Formato | Limite (decodificado) |
|-------|---------|-----------------------|
| `cnab_base64` | CNAB 400 remessa (`.rem`/`.txt`) em base64 | 16 MB |
| `nfe_zip_base64` | ZIP com XMLs de NFe em base64 | 32 MB |

Arquivos acima do limite, base64 inválido ou CNAB que não parseia retornam `400 validation_error` — e nada é cobrado, já que o débito só acontece após o parse. Veja [Erros](/credito/erros).

## Próximos passos

<CardGroup cols={3}>
  <Card title="Ciclo de vida" icon="arrows-rotate" href="/credito/ciclo-de-vida">
    Como o verdict de cada título é consolidado
  </Card>
  <Card title="Engines" icon="gears" href="/credito/engines">
    A política que decide o efeito de cada validação
  </Card>
  <Card title="Quickstart" icon="rocket" href="/credito/quickstart">
    O fluxo completo, do base64 ao drill-down
  </Card>
</CardGroup>
